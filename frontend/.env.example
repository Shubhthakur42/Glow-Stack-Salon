# Set this to the same value as backend ADMIN_KEY to allow the Admin page to fetch inquiries
REACT_APP_ADMIN_KEY=your-admin-key

# If proxy is not working, point directly to backend origin
# Example: REACT_APP_API_BASE=http://localhost:5000

# Example: REACT_APP_ADMIN_KEY=secret123
# Note: You must restart the dev server after changing .env files.